{
  "name": "top-news",
  "version": "0.1.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build-linux-n": "electron-packager ./ --out ./c --platform=linux --arch=x64 --overwrite --icon=icon.ico",
    "build-linux": "electron-packager ./ --out ./c --platform=linux --arch=x64 --overwrite --asar --icon=icon.ico",
    "build-window": "electron-packager ./ --out ./c --platform=win32 --arch=x64 --overwrite --asar"
  },
  "dependencies": {
    "puppeteer": "^5.3.1",
    "electron": "^10.1.2",
    "electron-packager": "^15.1.0",
    "electron-store": "^6.0.0",
    "express": "^4.17.1"
  }
}
